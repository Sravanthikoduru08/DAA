def sort_list(nums):
    if not nums:  
        return nums
    return sorted(nums)  


print(sort_list([]))  
print(sort_list([1]))  
print(sort_list([7, 7, 7, 7]))  
print(sort_list([-5, -1, -3, -2, -4]))  
